header.header
    nav.nav.nav--menus
        a.nav__el(href="/") All tasks
    nav.nav.nav--user 
        if user
            a.nav__el.nav__el--logout Log out
            a.nav__el(href="/") 
                span.nav__user-name= user.username 
        else
            a.nav__btn(href="/login") Log in 
            a.nav__btn.nav__el--cta(href="/signup") Sign up